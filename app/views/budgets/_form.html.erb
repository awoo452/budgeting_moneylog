<%= form_with(model: budget) do |form| %>
  <% if budget.errors.any? %>
    <div>
      <h2><%= pluralize(budget.errors.count, "error") %> prohibited this budget from being saved:</h2>
      <ul>
        <% budget.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category_id, "Category" %><br>
    <%= form.collection_select :category_id, Category.where(kind: "expense").order(:name), :id, :name, prompt: "Select category" %>
  </div>

  <div>
    <%= form.label :month %><br>
    <%= form.month_field :month, value: budget.month %>
  </div>

  <div>
    <%= form.label :amount %><br>
    <%= form.number_field :amount, step: "0.01", min: "0.01" %>
  </div>

  <div>
    <%= form.label :notes %><br>
    <%= form.text_area :notes, rows: 4 %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
